apiVersion: v1
kind: Service
metadata:
  name: restaurant-service
  namespace: restaurant-system
  labels:
    app: restaurant-service
spec:
  type: ClusterIP
  selector:
    app: restaurant-service
  ports:
  - port: 3001
    targetPort: 3001
    name: http
