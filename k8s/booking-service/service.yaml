apiVersion: v1
kind: Service
metadata:
  name: booking-service
  namespace: restaurant-system
  labels:
    app: booking-service
spec:
  type: ClusterIP
  selector:
    app: booking-service
  ports:
  - port: 8002
    targetPort: 8002
    name: http
